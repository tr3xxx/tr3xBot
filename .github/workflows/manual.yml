
name: ' ' 

on:
  workflow_dispatch:
    inputs:
      name:
        description: ' '
        default: ' '
        required: true

jobs:
  greet:
    runs-on: ubuntu-latest

    steps:
    - name: ' '
      run: echo ' '
